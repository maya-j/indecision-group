'''using scikit learn train test split 
to split the data into training set and test set
'''
import numpy as np
from sklearn.model_selection import train_test_split

sklearn.model_selection.train_test_split(**arrays, **options) ->list

#get the data in the format it appears
#define the 80-20 range for training and text data
x_train, x_test, y_train, y_test = train_test_split(#insert data, #insert data)


